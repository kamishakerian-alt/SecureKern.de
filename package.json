{
  "name": "securekern-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/clean.mjs && eleventy",
    "clean": "node scripts/clean.mjs",
    "dev": "eleventy --serve --watch",
    "serve": "npx -y serve Website",
    "preview": "npm run build && npx -y serve Website",
    "cms": "decap-server"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "decap-server": "^3.5.0",
    "markdown-it": "^14.1.0",
    "sharp": "^0.34.5"
  }
}
