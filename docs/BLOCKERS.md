# Primary Blocker Register

Blockers that prevent or significantly hinder progress. Update as items are resolved or new ones appear.

| ID  | Description                                                                                                          | Impact                                       | Status |
| --- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------ |
| B1  | EN navigation service links point to `/en/*.html`; actual pages are at `/en/services/*.html` → 404s                  | Users cannot reach EN service pages from nav | Open   |
| B2  | Decap CMS “Einstellungen” (Navigation/Site) reference deleted `navigation.json` / `site.json`; data is now in `.cjs` | CMS settings cannot load/save                | Open   |

_Resolved blockers can be moved to a “Resolved” section below with date and fix._
